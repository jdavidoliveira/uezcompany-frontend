export enum STATUS {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum USERTYPE {
  BOTH = "BOTH",
  CLIENT = "CLIENT",
  UEZER = "UEZER",
}

export type UserType = "UEZER" | "BOTH" | "CLIENT"
